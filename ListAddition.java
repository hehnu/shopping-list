import java.util.Scanner;
abstract class ListAddition{
  

 
    
    double a, b, s;

    abstract void input();

    abstract void multiply();
    abstract void result();

    

Scanner enternum = new Scanner (System.in);

   //public void sleep(Scanner sc) {
       // this.name = sc.nextLine();
       // System.out.println(name + " snores really loud");
    //}
    //ListAddition.getScanner().close();
  }

//public void

// public double findTotal()
  //{
    //double sum = 0.00;
  //  int index = 0;
  //  while (index < list.length)
  //  {
  //    sum = sum + list[index].getPrice();
  //    index = index + 1;
  //  }
   // return sum;
 // }
